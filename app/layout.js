export const metadata = { title: "API Search Demo", description: "Secure server-side API integration demo" };

export default function RootLayout({ children }) {
  return <html lang="en"><body style={{margin:0,fontFamily:"system-ui, sans-serif",background:"#f5f7fb"}}>{children}</body></html>;
}
